/**
 * Workspace Apps client package: the React-free controller, its bare
 * observable store, and the client-safe contracts. The shell (Task 11) and the
 * Settings tab (Task 12) construct the controller with the real remote, slot
 * ledger, and graph-convergence seams; nothing here imports React.
 * @module @deepseek-ai/dsh-client-ui-page-app-manager
 */

export * from './client/index.ts'
